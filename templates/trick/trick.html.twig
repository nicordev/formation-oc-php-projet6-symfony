{% extends 'base.html.twig' %}

{% block title %}{{ trick.name }}{% endblock %}

{% block header %}
    <header>
        <figure>
            <img src="https://via.placeholder.com/1000x500?text=MainImage" alt="Image principale">
        </figure>
        <h1>{{ trick.name }}</h1>
    </header>
{% endblock %}

{% block body %}
    <div class="commands-wrapper">
        <a class="command" href="{{ path("edit_trick", {"id": trick.id}) }}">🖉</a><a class="command" href="{{ path("delete_trick", {"id": trick.id}) }}">🗑</a>
    </div>

    {# Medias #}
    <div class="trick-medias">
        {# TODO: insert images and videos #}
    </div>

    {# Description #}
    <div class="trick-description">
        {{ trick.description|raw }}
    </div>

    {# Info #}
    <div class="trick-metadata">
        {% if trick.getTrickGroups() %}
            <div>
            {% for trickGroup in trick.getTrickGroups() %}
                <a href="#">{{ trickGroup.getName() }}</a>
            {% endfor %}
            </div>
        {% endif %}
        <span class="metadata">Trick créé le {{ trick.createdAt|date('d/m/Y') }} à {{ trick.createdAt|date('H:i') }}</span>
        {% if trick.modifiedAt %}
            <span class="metadata">Modifié le {{ trick.modifiedAt|date('d/m/Y') }} à {{ trick.modifiedAt|date('H:i') }}</span>
        {% endif %}
    </div>

    {# Comments #}
    <div class="trick-comments">
        {# TODO: insert comments and new comment form #}
    </div>
{% endblock %}
