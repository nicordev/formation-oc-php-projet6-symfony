{# Navigation bar #}
<nav id="main-navigation-bar" class="navbar fixed-top navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="/">SnowTricks</a>
    <button id="main-navigation-bar-menu-button" class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item{% if app.session.get("current_page") is same as("home") %} active{% endif %}">
                <a class="nav-link" href="/">Accueil {% if app.session.get("current_page") is same as("home") %}<span class="sr-only">(current)</span>{% endif %}</a>
            </li>
            <li class="nav-item{% if app.session.get("current_page") is same as("trick_editor") %} active{% endif %}">
                <a class="nav-link" href="{{ path('trick_add') }}">New trick {% if app.session.get("current_page") is same as("trick_editor") %}<span class="sr-only">(current)</span>{% endif %}</a>
            </li>
            {% if is_granted('ROLE_MODERATOR') %}
                <li class="nav-item{% if app.session.get("current_page") is same as("moderation") %} active{% endif %}">
                    <a class="nav-link" href="{{ path('moderation_panel') }}">Modération {% if app.session.get("current_page") is same as("moderation") %}<span class="sr-only">(current)</span>{% endif %}</a>
                </li>
            {% endif %}
            {% set connected = false %}
            {% include 'member/navConnection.twig' %}
        </ul>
        <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="text" placeholder="Trick">
            <button class="btn btn-secondary my-2 my-sm-0" type="submit">Rechercher</button>
        </form>
    </div>
</nav>
