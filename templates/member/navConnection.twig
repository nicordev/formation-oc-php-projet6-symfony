{% if not app.user %}
    <li class="nav-item{% if app.session.get("current_page") is same as("registration") %} active{% endif %}">
        <a class="nav-link" href="{{ path("registration_route") }}">Inscription {% if app.session.get("current_page") is same as("registration") %}<span class="sr-only">(current)</span>{% endif %}</a>
    </li>
    <li class="nav-item{% if app.session.get("current_page") is same as("login") %} active{% endif %}">
        <a class="nav-link" href="{{ path("login_route") }}">Connexion {% if app.session.get("current_page") is same as("login") %}<span class="sr-only">(current)</span>{% endif %}</a>
    </li>
{% else %}
    <li class="nav-item">
        <a class="nav-link" href="#">Mon profil</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" href="{{ path("logout_route") }}">Déconnexion</a>
    </li>
{% endif %}